{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm turbo run build --filter=portal",
  "ignoreCommand": "pnpm turbo query affected --packages portal --tasks build --exit-code"
}
