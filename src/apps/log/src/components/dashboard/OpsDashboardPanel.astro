---
import { HourlyChart } from "@/components/dashboard/HourlyChart";

type SeriesItem = {
  label: string;
  count: number;
};

const { series } = Astro.props as { series: SeriesItem[] };
---

<HourlyChart points={series} client:load />
