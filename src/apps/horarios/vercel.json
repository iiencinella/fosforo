{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm turbo run build --filter=horarios",
  "ignoreCommand": "pnpm turbo query affected --packages horarios --tasks build --exit-code"
}
