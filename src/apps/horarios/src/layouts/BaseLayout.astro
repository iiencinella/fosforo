---
import Head from "@/components/Head.astro";

type Props = {
  title: string;
  description?: string;
  image?: string;
};

const {
  title,
  description = "Consulta horarios de Misa y celebraciones por ciudad, templo y cercania.",
  image,
} = Astro.props as Props;
---

<!doctype html>
<html lang="es">
  <Head title={title} description={description} image={image} />
  <body class="horarios-app">
    <a class="skip-link" href="#main-content">Saltar al contenido</a>
    <div class="app-shell" transition:animate="fade">
      <slot />
    </div>
  </body>
</html>
